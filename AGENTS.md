# Agent pointer

Read `CLAUDE.md` at the repository root first, then the `CLAUDE.md` or
`README.md` inside the app you are changing (`apps/<app>/`). Those files are the
source of truth for layout, constraints and commands.
