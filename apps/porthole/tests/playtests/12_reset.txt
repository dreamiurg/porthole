# Start over (Pets Club): a long press under the stats opens the question; No keeps the pup, Yes lets it go and the
# adoption starts again. Only the pup goes: the profile stays, and the store forgets the pup too.
newgame Sam Biscuit
wait 2000
tap 40 27
wait 200
expect-screen stats
hold 80 148
wait 100
expect-screen confirm_reset
snap 01_confirm
tap 45 102
wait 200
expect-screen stats
expect food=70
hold 80 148
wait 100
tap 115 102
wait 300
expect-screen intro
expect profile=0/1
snap 02_intro_again
# adopting again right away: the new pup is what gets saved, not the pending erase
tap 80 80
wait 1000
tap 80 80
wait 200
tap 123 28
tap 92 144
wait 200
tap 32 78
wait 900
tap 80 80
wait 300
expect-screen home
tap 80 12
wait 200
expect-screen launcher
tap 80 76
wait 2000
expect-screen home
# start over once more, straight out to the launcher and back in: the parcel, nothing was left in the store
tap 40 27
wait 200
hold 80 148
wait 100
tap 115 102
wait 300
expect-screen intro
tap 80 12
wait 200
expect-screen launcher
tap 80 76
wait 2000
expect-screen intro
sheet reset
